#include "main.h"
#include<stdio.h>
void _puts(char *str);
/**
 * main - Entry point
 * _puts - changes string to puts
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
