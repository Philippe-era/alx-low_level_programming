#include "main.h"


/**
* _isdigit - numbers 1 - 9 to be checked
* @c: number to be checked
* Return: checks if there is a  number from 1 - 9
*/

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
