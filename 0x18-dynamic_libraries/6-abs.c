#include "main.h"

/**
 * _abs -displays to be checked and all
 * @n: number to be compared with conditions statemet
 * Return: n is an inteerger
 */

int _abs(int n)
{
	int success_check = 0;

	if (n > success_check)
	{
	return (n);
	}
	else if (n < success_check)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
